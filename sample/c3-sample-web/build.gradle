configurations {
    all*.exclude group: 'org.slf4j', module: 'log4j-over-slf4j'
    all*.exclude group: 'ch.qos.logback', module: 'logback-core'
}

mainClassName = 'com.c3.sample.web.Application'

dependencies {
  compile project(":c3-base-menu")
  compile project(":c3-base-file")
}
